import Typography from '../../components/typography';

export default function View() {
  return (
      <div>
        <Typography type="display" $size="lg">Connections</Typography>
      </div>
  );
}