import motion from './motion';

const config = {
  motion,
  dev: {
    xs: Math.random() * 300,
    sm: Math.random() * 500,
    md: Math.random() * 800,
    lg: Math.random() * 1000,
  },
};

export default config;
